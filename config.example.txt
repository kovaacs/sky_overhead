# Copy this file to /config.txt on the microSD card and replace these values.
SSID=your-wifi-name
PASS=your-wifi-password
LAT=47.4979
LON=19.0402
ALT=100
TZ=CET-1CEST,M3.5.0,M10.5.0/3

# Display units and aircraft search radius.
SPEED=kph
HEIGHT=ftfl
TEMP=c
RADIUS=30

# Refresh behavior. BUSY is constrained to 15-600 seconds. MAX_REFRESH=0 disables
# forced redraws; positive values set a global maximum time between redraws.
# Remove NIGHT_MODE or leave it empty to disable quiet hours.
NIGHT_MODE=23:00-07:00
BUSY=60
MAX_REFRESH=0
DEMO=0

# Optional local readsb/tar1090 fallback. The firmware appends /data/aircraft.json.
# LOCAL_ADSB_URL=http://192.168.1.20:8080
